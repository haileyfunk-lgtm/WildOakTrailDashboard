.secrets/
.cache/
.streamlit/secrets.toml
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
*.v1-backup
.claude/settings.local.json
.DS_Store
Thumbs.db

# Personal config — users copy config.example.py to config.py and edit locally
config.py

# Google OAuth secrets — never commit
credentials.json
token.json
data/bookkeeping.json

# Internal dev docs
DESIGN.md
HANDOFF.md
NOTES.md

# Personal side-utilities (not used by the dashboard; hardcoded personal paths)
yt.py
hn.py
_oauth_once.py
assets/_recolor.py

# Andres Properties site — never commit server credentials or SSH keys
andres-properties-site/credentials/
*.pem
id_rsa*
*_ssh_key
*_ssh_key.pub
